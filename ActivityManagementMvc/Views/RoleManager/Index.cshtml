@model ActivityManagement.ViewModels.Home.HomeViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "_Layout";
}


<div id="modal-placeholder"></div>
@await Html.PartialAsync("_Header", Model.BreadCrumbViewModel)

<div class="container-fluid mt--7">
    <div class="row mt-5">
        <div class="card shadow w-100">
            <div class="card-header font_Vazir_Medium">
                مدیریت نقش ها
            </div>
            <div class="card-body">
                <form asp-controller="RoleManager">
                    @await Html.PartialAsync("_RoleTable")
                </form>
            </div>
        </div>
    </div>
</div>


